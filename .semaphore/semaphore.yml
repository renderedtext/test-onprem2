version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: s1-linux-aws-pool
    os_image: ''
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - ls -lah
            - sudo lshw
          parallelism: 10
